name: Testing

on:
  push:
    branches:
      - 'master'
  pull_request:

jobs:
  test:
    name: Testing
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: Run regression tests
      run: bash ./test.sh
